name: Massar Auto Core Check

on:
  push:
    branches:
      - feature/auto-core-v1
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: massar-auto-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/postgres
      NEXT_TELEMETRY_DISABLED: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Link warranty history context
        run: python scripts/link-warranty-history-context.py

      - name: Verify warranty history context
        run: |
          grep -F 'vehicleWarrantyClaimHistoryService.getVehicleWarrantyClaimHistory' 'app/vehicles/[id]/_vehicle-history.tsx'
          grep -F 'serviceWarrantyLinkService.getWarrantyFollowUpLink' 'app/service-orders/[id]/page.tsx'
          grep -F 'الضمان والعودة للصيانة' 'app/vehicles/[id]/_vehicle-history.tsx'
          grep -F 'أمر متابعة ضمان / عودة للصيانة' 'app/service-orders/[id]/page.tsx'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Prisma validate and generate
        run: |
          npx prisma validate
          npx prisma generate

      - name: TypeScript
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Commit warranty history context
        if: github.event_name == 'push'
        run: |
          if git diff --quiet -- 'app/vehicles/[id]/_vehicle-history.tsx' 'app/service-orders/[id]/page.tsx'; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add 'app/vehicles/[id]/_vehicle-history.tsx' 'app/service-orders/[id]/page.tsx'
          git commit -m 'feat(auto): link warranty claims to vehicle and follow-up history'
          git push origin HEAD:feature/auto-core-v1
