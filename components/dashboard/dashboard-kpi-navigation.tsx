export function DashboardKpiNavigation() {
  // Dashboard KPI cards are native Next.js links in Massar Auto.
  // The old DOM listener targeted phone-repair labels and routes, so keeping it
  // active could only introduce stale navigation behavior.
  return null;
}
